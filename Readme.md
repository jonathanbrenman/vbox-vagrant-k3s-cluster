# K3s Centos with Vagrant

## Requirements
* Install Vagrant, Ansible, Virtualbox, Make
* Change in Vagrant file /home/<your user>/.ssh/id_rsa.pub

## Create cluster
```
    make create
```

## Delete cluster
```
    make destroy
```